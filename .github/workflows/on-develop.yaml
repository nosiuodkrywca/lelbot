name: LelBot dev CI/CD

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  deploy_on_prod:

    environment: dev
    runs-on: newton
    
    steps:
    
      - uses: actions/checkout@v3
      
      - name: create-json
        uses: jsdaniell/create-json@1.1.2
        with:
           name: "config.json"
           json: '{"token":"${{ secrets.DISCORD_TOKEN }}"}'

#      - uses: actions/setup-node@v3
#        with:
#          node-version: 18
      
#      - run: npm install pm2@latest -g
      
      - run: npm install
      
      ### hotfix, bug in glob
      - run: npm install --save glob
      ###

      - run: pm2 delete kekbot 2> /dev/null || true

      - run: RUNNER_TRACKING_ID="lelbot_dev" && (nohup pm2 start index.js -i 1 -f --name kekbot&)

      - run: pm2 save
